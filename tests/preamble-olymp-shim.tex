% Exercises the olymp.sty compatibility shim: an existing document that says
% \usepackage{olymp} must still build. %%OPTS%% is substituted by tests/run.sh.
\documentclass[11pt, a4paper, oneside]{article}
\usepackage[%%OPTS%%]{olymp}
